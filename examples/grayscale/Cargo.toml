[package]
name = "grayscale"
version = "0.1.0"
edition = "2024"

[dependencies]
cust = { path = "../../crates/cust" }
image = "0.25.9"
nanorand = "0.7"

[build-dependencies]
cuda_builder = { workspace = true, default-features = false }
